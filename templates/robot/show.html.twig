{% extends 'base.html.twig' %}

{% block title %}Robot{% endblock %}

{% block body %}
    <h1>Robot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ robot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ robot.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ robot.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ robot.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ robot.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_robot_index') }}">back to list</a>

    <a href="{{ path('app_robot_edit', {'id': robot.id}) }}">edit</a>

    {{ include('robot/_delete_form.html.twig') }}
{% endblock %}
